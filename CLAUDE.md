# Claude 작업 지침

## 1. 핵심 원칙
1. 사용자가 지시한 사항만 수행한다
2. 임의로 기능을 추가하거나 변경하지 않는다
3. 코딩 전 구조를 먼저 제시하고 승인받는다
4. 증거 없이 "동일합니다" 말하지 않는다
5. 코딩 작업전에 충분히 기존 파일 확인하고 중복함수, 중복 변수 , 중복 로직 없도록 확인
---

## 2. 아키텍처 원칙 (절대 위반 금지)

### UI와 로직 분리
| 파일 | 역할 | 금지 사항 |
|------|------|----------|
| `main_window.py` | UI 이벤트 핸들링, 표시 업데이트 | 비즈니스 로직 작성 금지 |
| `services/*.py` | 비즈니스 로직, 데이터 처리 | UI 직접 조작 금지 |
| `job_executor.py` | Job 실행 엔진 | UI 직접 조작 금지 |


## 3. 코딩 규칙

### 금지 사항
- 사용자가 요청하지 않은 코드 수정
- 빌드 설정 수정 (tasks.json, CMakeLists.txt)
- 기존 승인된 로직 임의 변경

### 코딩 전 확인
- 동일/유사 함수·변수가 이미 존재하는지 확인
- 중복 발견 시 기존 코드 재사용 또는 보고

---

## 4. 작업 종료 보고 양식
```
## 다짐 및 확인
- [ ] 사용자 요청 사항만 수행했는가?
- [ ] 임의로 변경한 부분이 없는가?
- [ ] 증거 없이 "동일합니다" 말하지 않았는가?
- [ ] 아키텍처 원칙 준수 (UI/로직 분리)
- [ ] 동일/유사 함수·변수 중복 확인
- [ ] (ROS 변환 시) 제어 루프 순서 동일한가?
- [ ] (파일 수정 시) git add로 staging 했는가?

## 수행한 작업
- (작업 내용)
```

